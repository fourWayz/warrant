# Track B reference flow

An isolated, real 0G Compute session run against Galileo testnet, used
only to confirm the M3 compatibility finding against actual observed
behavior. See `docs/compute-compatibility-finding.md` and
`docs/m3-tracks.md` at the repo root for the finding and the full run
history with transaction hashes.

This EOA and its funds have no relationship to WarrantModule or
WarrantRegistry. Nothing here is Warrant-governed.

```
TRACK_B_KEY=0x... node run.mjs
```
